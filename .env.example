# SQLAlchemy / psycopg3 URL (used by Alembic migrations)
# En macOS con Postgres local, suele hacer falta un rol que exista (p. ej. tu usuario) y la DB creada (`createdb vixion`).
DATABASE_URL=postgresql+psycopg://vixion:vixion@localhost:5432/vixion
